<h1>result</h1>

  <table>
    <tr>
      <th>タイム</th>
      <th>大会名</th>

    </tr>
    <% @entrys.each do |entry| %>
    <tr>
      <th>
        <%= Time.at(entry.time).strftime("%H時間%M分%S秒")%>
      </th>
      <th><%= link_to entry.event.name, event_path(entry.event_id) %></th>

　    </tr>
    <% end %>
  </table>