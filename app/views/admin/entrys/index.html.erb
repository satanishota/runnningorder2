<h1>Admin::Entrys#index</h1>

<p>
  <%= link_to "rank", admin_rank_path(:event_id => @entrys.first.event_id) %>
<p>

<% @entrys.each do |entry| %>
  <p>タイム</p>
  <p>
    <%= link_to entry.time, admin_entry_path(entry.id) %>
  <p>
  <p>大会ID</p>
  <p><%= entry.event_id %><p>
  <p>ユーザーID</p>
  <p><%= entry.user_id %><p>
  ---------------------------------------------------------
<% end %>

