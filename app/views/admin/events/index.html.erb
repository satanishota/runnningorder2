<h1>Admin::Events#index</h1>

<% @events.each do |event| %>
  <p>タイトル</p>
  <p>
    <%= link_to event.name, admin_event_path(event.id) %>
  <p>
  <p>開催日</p>
  <p><%= event.day %><p>
  <p>距離</p>
  <p><%= event.distance %>㎞<p>
<% end %>

<li>
  <%= link_to "新規登録", new_admin_event_path %>
</li>